export async function queryOpenRouter(model, messages) { const apiKey = process.env.OPENROUTER_API_KEY || localStorage.getItem("openrouter_api_key"); if (!apiKey) { throw new Error("OpenRouter API key not configured"); } const response = await fetch("https://openrouter.ai/api/v1/chat/completions", { method: "POST", headers: { "Authorization": \`Bearer \${apiKey}\`, "HTTP-Referer": "https://vitachain.vercel.app", "Content-Type": "application/json" }, body: JSON.stringify({ model, messages, temperature: 0.3, max_tokens: 2000 }) }); if (!response.ok) { throw new Error(\`OpenRouter error: \${response.status}\`); } const data = await response.json(); return data.choices[0].message; } export async function listOpenRouterModels() { const apiKey = process.env.OPENROUTER_API_KEY || localStorage.getItem("openrouter_api_key"); const response = await fetch("https://openrouter.ai/api/v1/models", { headers: { "Authorization": \`Bearer \${apiKey}\` } }); const data = await response.json(); return data.data; }
