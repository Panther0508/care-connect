import { useEffect, useState } from " react\; import { getQuotaRemaining, getQuotaUsed } from \../services/aiQuotaManager\; export default function AIQuotaIndicator({ userRole = \global\ }) { const [remaining, setRemaining] = useState(2); const [used, setUsed] = useState(0); useEffect(() => { setRemaining(getQuotaRemaining(userRole)); setUsed(getQuotaUsed(userRole)); }, [userRole]); if (remaining === 2) return null; return ( <div className=\fixed bottom-20 right-4 z-50\> <div className=\bg-slate-800 border border-teal-500/30 rounded-lg px-3 py-2 text-sm\> <span className=\text-slate-400\>AI Quota: </span> <span className=\text-teal-400 font-medium\>{remaining}</span> <span className=\text-slate-500\>/2 remaining</span> </div> </div> ); }
