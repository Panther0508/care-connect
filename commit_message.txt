Phase 5 Part 1: UI/UX finalization and verification

- Text-size enforcement: Upgrade text-xs/text-sm to text-base across key pages; add global min-font rule for icon-label patterns.
- Settings tab navigation: Convert to horizontal scroll with flex-shrink-0, min-w-[90px], increased padding, gradient fade indicator.
- Profile editing: Comprehensive ProfileTab with full fields (display name, phone, gender, biological sex, DOB, height, weight, activity level, language) and photo upload to IndexedDB + Clerk publicMetadata.
- Passport AI: Already routed through hybrid router; QR generation now uses LoadingSpinner during generation.
- LoadingSpinner component: Create with VitaAvatar float + rotating teal ring; replace plain loading indicators in AIAssistant and Passport.
- PWA install: Add usePWAInstall hook integration; show Install VitaChain button in PatientDashboard and Settings (Install tab) with iOS instructions.
- Playwright verification: Create tests/screenshots/verify-fixes.spec.mjs capturing key screenshots (landing, settings tabs, profile editing, passport AI indicator, upgraded spinner).
- Documentation: Update README and security/conduct docs with correct contact email.
- Verify all utility pages exist and routed; audit full page inventory for design compliance (glassmorphism, palette, spacing, focus rings, skeletons, empty/error states).
- Run build successfully; no TypeScript errors.

Phase 5 Part 2: Premium animations and effects (highest tier)

- Install & use framer-motion (v12) for production-grade motion design.
- Add custom ScrollReveal component with intersection-triggered opacity/translate/scale using cubic-bezier easing.
- Add MagneticButton component: spring-based magnetic pull on hover with press scale.
- Global CSS utilities: stagger-child delays, parallax targets, mesh-gradient, grain overlay, tilt-card transforms, premium bounce/hover, reveal-on-scroll, blur-in, gradient-shimmer, pop-in, floating orbs.
- Page transitions: Upgrade App PageWrapper to multi-axis blur + scale + spring variants.
- Landing page: floating ambient orbs with float animation, parallax background glows, magnetic CTA buttons, staggered feature badges.
- AIAssistant: blur-in morph transitions for messages, staggered reveal for actions (reasoning/citations), springy loading indicators.
- HealthGraph: staggered card list animations, premium modal with backdrop blur and spring scale, ScrollReveal sections.
- Passport: ScrollReveal for all sections, magnetic download/share buttons, staggered health summary preview, QR pop-in.
- ClinicianView: staggered stat cards and patient list, magnetic add-patient button.
- Build verified clean; all animations respect prefers-reduced-motion and reduced-transparency.
- Commit without freezing codebase.